# `MergeStrategy` & `deep_merge`

Merge strategies and the low-level merge function used internally by
[`Config`](config.md).

## MergeStrategy

::: strata.MergeStrategy
    options:
      show_source: true
      members:
        - REPLACE
        - ADDITIVE
        - TYPESAFE

## `deep_merge()`

::: strata._merge.deep_merge
    options:
      show_source: true

